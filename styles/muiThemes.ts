import { createTheme } from '@mui/material';

const globalTheme = createTheme({
  palette: {},
  typography: {
    allVariants: {
      fontFamily: "'Raleway', sans-serif"
    }
  }
});

export { globalTheme };
